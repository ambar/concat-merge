{
  "name": "concat-merge",
  "version": "1.0.3",
  "description": "Recursively merge objects",
  "license": "MIT",
  "module": "index.js",
  "main": "index.cjs.js",
  "scripts": {
    "prepare": "npm test && npm run build",
    "build": "esbuild cjs.js --bundle --minify-syntax --format=cjs --outfile=index.cjs.js",
    "test": "vitest",
    "test:coverage": "vitest run --coverage --coverage.exclude .*.js --coverage.exclude index.cjs.js"
  },
  "files": [
    "index.cjs.js",
    "index.js"
  ],
  "keywords": [
    "gogen",
    "concat",
    "merge"
  ],
  "devDependencies": {
    "@vitest/coverage-v8": "^1.4.0",
    "vitest": "^1.4.0"
  }
}
